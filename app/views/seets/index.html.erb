<div class="container">
 <div class="row">
   <div class="w-50 mx-auto">
<h1 class="mt-5 mb-3">ベスト5</h1>

 <% @seets.each.with_index(1) do |seet, idx| %>

  <div class="font-weight-bold mb-3 text-warning"><%= idx %>. <%= seet.shop_name %></div>

  <div class="font-weight-bold"></div>
 <%= link_to seet_path(seet.id) do %>
  <div><%= attachment_image_tag seet, :image, format: 'jpeg', size: "600x400" %></div>
 <% end %>
  <div><%= seet.caption %></div>

  <div class="mb-5"><%= raw Rinku.auto_link(h(seet.rinku)) %></div>

    <% end %>
    </div>
   </div>
  </div>